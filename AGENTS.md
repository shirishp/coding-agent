# coding-agent

Tools live in `tools/`, the model loop in `utils/loop.py`, evals in `evals/`.

Sample hooks and skills are templates in `examples/`. Copy them into `.agent/` to enable; that directory is local runtime and gitignored.
